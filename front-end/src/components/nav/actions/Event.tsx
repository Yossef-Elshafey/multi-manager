import { Overlay } from "../../Overlay";
import { UserNavActionsProps } from "../UserNavActions";

export const EvAction = ({ setAction }: UserNavActionsProps) => {
  return <Overlay>Haha</Overlay>;
};
